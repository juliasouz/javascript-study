/* Use this technique (example: Math.floor(Math.random() * 20);)
to generate and return a random whole number in the range from 0 to 9. */

function randomWholeNum() {
    return Math.floor(Math.random() * 10);
}